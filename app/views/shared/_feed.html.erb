<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %> <!-- TODO 因为@feed_items都是Micropost的对象，所以，会找microposts/_micropost.html.erb -->
  </ol>
  <%= will_paginate @feed_items %>
<% end %>